# Hyprland color samples

general {
  col.shadow = rgba(33ccff)
  col.active_border = rgba(33ccffee)
  col.inactive_border = rgba(595959aa)
}

# NOTE: short hex-like values are not Hyprland colors for this matcher.
misc {
  example = rgba(f0ae)
}
