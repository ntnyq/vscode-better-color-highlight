# Hyprland color samples

general {
  col.active_border = rgba(33ccffee)
  col.inactive_border = rgba(595959aa)
}

# Short hex-like values are not Hyprland colors for this matcher.
misc {
  example = rgba(f0ae)
}
